(ns org.rusbenk.cerberus-api.core
  (:gen-class))

;; Namespace de rotas
;; Namespace de Handlers
;; Namespace de config
;; - Setup do server
;; - Setup dos interceptors default
;; - Setup de parsing/coercion

(defn -main [& _]
  (println "Hello World"))
